<div id="app" class="mx-auto py-5 px-5">
  <div class="w-70 rounded bg-white p-5 flex justify-center">
    <div>
      <h2 class="text-center uppercase mb-1 font-semibold">TMIVT2026 Women's Division</h2>
      <h2 class="text-center mb-5 uppercase text-sm font-semibold">{{position}}</h2>

      <h2 class="text-center uppercase text-sm font-semibold">{{name}}</h2>
      
    </div>
  </div>
</div>

<script>
  const { createApp, ref, onMounted, computed } = Vue;

  createApp({
    setup() {
        const position = ref("Queen of the tournament")
      const name = ref("Fathimath Afaaf")
      const socket = io("http://localhost:7065");
      //const socket = io("https://volley.aliareef.com");

      socket.on("TopSix", async (data) => {
        console.log("Score updated:", data);
        names.value = data;
        console.log(names.value);
      });

      return {
        position,
        name
      };
    },
  }).mount("#app");
</script>
